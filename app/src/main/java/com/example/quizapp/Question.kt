package com.example.quizapp

class Question(questionNumber:Int, questionText:String, questionImageRes:Int, questionAnswer:String) {
    var number = questionNumber
    var text = questionText
    var image = questionImageRes
    var answer = questionAnswer
}